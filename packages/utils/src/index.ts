import aaa from './a.json';

/**
 * @description This is a function that logs "a"
 */
export const a = (text: string) => {
	console.log(aaa);
	console.log('b');
	console.log('========>', text);
	const a = 1;
	return a;
};
